# 使用相对路径，同网段设备访问时 API 会发往当前主机:5173，由 Vite 代理到后端
# 若仅本机访问且希望直连后端，可改为 http://localhost:8000/api/v1
VITE_API_BASE_URL=/api/v1
# MinIO 通过 Nginx/Vite 代理的 /minio/ 路径访问，不再直连 9000
VITE_MINIO_PUBLIC_URL=/minio
VITE_MINIO_BUCKET_NAME=spareparts